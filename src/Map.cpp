#include "Map.hpp"

void Map::doProcess(int32_t elapsed) {

  // server->travel(obj, "map_string")
  // object->travelTo("map_string", -1, -1)
    // travelTo
      // gTravelAgent->moveObject(obj, "map_string", -1, -1)
        // Map* map = gMapAgent->acquireMap("map_string")
        // map->addObject(obj, -1, -1);
}
